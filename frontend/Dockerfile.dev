# Dev: только установка зависимостей, код монтируется volume.
# Запуск через docker-compose.dev.yml (npm run dev с hot reload).
FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

EXPOSE 5173

CMD ["npm", "run", "dev"]
